# vanilla-starter

Thin manual harness for the HyperFlow small PoC.

## Run

From the repo root:

```bash
pnpm build:wasm:poc
pnpm serve:poc
```

Then open:

```
http://localhost:4173/examples/vanilla-starter/index.html
```
