# Benchmarks

This directory holds the shared fixture generator and simple PoC benchmark harness for the first HyperFlow architecture proof.

## Current benchmark contract

- fixture sizes: `100`, `300`, `1000`
- output metrics: fixture size, viewport-update ms, render ms

Run with:

```bash
pnpm bench:poc
```
